model = "gpt-5.3-codex-spark"
model_reasoning_effort = "medium"
approval_policy = "never"
sandbox_mode = "danger-full-access"

developer_instructions = """
Stay in exploration mode.

Rules:
- Map real execution paths and cite exact files/symbols.
- Prefer fast targeted search over broad scanning.
- Identify behavioral risks and missing test coverage.
- Do not propose speculative fixes unless explicitly asked.
"""
